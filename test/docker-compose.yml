version: '3'

services:

  elasticsearch:
    image: sgaroncse/elasticsearch:7.1.0
    environment:
      ES_JAVA_OPTS: "-Xms512m -Xmx512m"
      DISCOVERY_TYPE: "single-node"
    ports:
      - '9200:9200'

  redis:
    image: redis
    ports:
      - '6379:6379'
      - '6380:6379'
