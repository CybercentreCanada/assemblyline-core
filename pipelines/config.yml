filestore:
  cache:
    - file:///var/cache/assemblyline/
  storage:
    - file:///var/cache/assemblyline/
core:
  redis:
    nonpersistent:
      host: localhost
      port: 6379
    persistent:
      host: localhost
      port: 6379
  rabbit_mq:
    host: localhost
    port: 5672
datastore:
  ilm:
    enabled: true
  hosts: ["localhost:9200"]